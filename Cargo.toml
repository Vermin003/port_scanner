[package]
name = "port_scanner"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
tokio = { version = "1.50.0", features = ["net", "sync", "time", "rt-multi-thread", "macros"]}
anyhow = "1.0.102"
